typedef enum {false,true} bool;
extern int __VERIFIER_nondet_int(void);

int main(){
  int x;
  int y;
  x = 0;
  y = 0;

  x = x + 2;
  
  assert(x == 2);

  if(y != 0) {
    assert(1 == 0);
  }

  assert((x + y) == 2);
  
  return 0;
}