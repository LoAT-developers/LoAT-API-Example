typedef enum {false,true} bool;
extern int __VERIFIER_nondet_int(void);

int main(){
  int x;
  x = 0;

  x = x + 2;

  if(x == 2) {
    assert(1 == 1);
  } else {
    assert(1 == 0);
  }

  assert(x != 1);

  return 0;
}