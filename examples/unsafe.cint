typedef enum {false,true} bool;
extern int __VERIFIER_nondet_int(void);
int main(){
  int x;
  int y;

  x = 0;

  while (x < 10) {
    x = x + 1;
    y = 1;
  }

  assert(x < 10);
  assert(y != 1);

  return 0;
}
 